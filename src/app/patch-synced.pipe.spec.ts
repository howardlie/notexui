import { PatchSyncedPipe } from './patch-synced.pipe';

describe('PatchSyncedPipe', () => {
  it('create an instance', () => {
    const pipe = new PatchSyncedPipe();
    expect(pipe).toBeTruthy();
  });
});
